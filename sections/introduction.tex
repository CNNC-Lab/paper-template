The introduction motivates the work and states the contribution. Cite with biblatex,
e.g.\ a foundational result~\autocite{example2025}. Section content lives in its own
file under \texttt{sections/} so it can be reordered or overridden per journal.
